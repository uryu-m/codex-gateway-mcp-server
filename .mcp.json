{
  "mcpServers": {
    "codex-gateway": {
      "command": "node",
      "args": ["dist/index.js"],
      "env": {
        "CODEX_SANDBOX": "workspace-write",
        "CODEX_APPROVAL": "on-request",
        "LOG_DIR": "logs"
      }
    }
  }
}
